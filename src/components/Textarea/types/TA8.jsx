import React from 'react';

import Textarea from '../Textarea';

const TA8 = props => (
  <Textarea
    size="large"
    showTextareaIcon
    showSymbolsCounter
    {...props}
  />
);

export default TA8;
