import React from 'react';

import Textarea from '../Textarea';

const TA6 = props => (
  <Textarea
    size="medium"
    showTextareaIcon
    showSymbolsCounter
    {...props}
  />
);

export default TA6;
