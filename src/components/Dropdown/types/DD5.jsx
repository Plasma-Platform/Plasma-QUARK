import React from 'react';

import Dropdown from '../Dropdown';

const DD5 = props => (
  <Dropdown
    type={5}
    showButton={false}
    showLabel={false}
    {...props}
  />
);

export default DD5;
