import React from 'react';

import Button from '../Button';

const B2G = props => (
  <Button
    widthType="full"
    heightType="medium"
    roundedType="all"
    bgType="2"
    {...props}
  />
);

export default B2G;
