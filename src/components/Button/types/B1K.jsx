import React from 'react';

import Button from '../Button';

const B1K = props => (
  <Button
    widthType="full"
    heightType="large"
    roundedType="bottom"
    bgType="1"
    {...props}
  />
);

export default B1K;
