import React from 'react';

import Button from '../Button';

const BF3 = props => (
  <Button
    widthType="auto"
    heightType="large"
    roundedType="all"
    bgType="facebook"
    icon="facebook"
    {...props}
  />
);

export default BF3;
