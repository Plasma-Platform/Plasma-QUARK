import React from 'react';

import Button from '../Button';

const BG6 = props => (
  <Button
    widthType="square"
    heightType="large"
    roundedType="all"
    bgType="google-plus"
    icon="google-plus"
    {...props}
  />
);

export default BG6;
