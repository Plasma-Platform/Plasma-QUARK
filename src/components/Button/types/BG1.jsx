import React from 'react';

import Button from '../Button';

const BG1 = props => (
  <Button
    widthType="auto"
    heightType="medium"
    roundedType="all"
    bgType="google-plus"
    icon="google-plus"
    {...props}
  />
);

export default BG1;
