import React from 'react';

import Button from '../Button';

const BF2 = props => (
  <Button
    widthType="full"
    heightType="medium"
    roundedType="all"
    bgType="facebook"
    icon="facebook"
    {...props}
  />
);

export default BF2;
