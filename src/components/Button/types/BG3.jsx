import React from 'react';

import Button from '../Button';

const BG3 = props => (
  <Button
    widthType="auto"
    heightType="large"
    roundedType="all"
    bgType="google-plus"
    icon="google-plus"
    {...props}
  />
);

export default BG3;
