import React from 'react';

import Button from '../Button';

const B3D = props => (
  <Button
    widthType="full"
    heightType="large"
    roundedType="all"
    bgType="3"
    {...props}
  />
);

export default B3D;
