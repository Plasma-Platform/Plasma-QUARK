import React from 'react';

import Button from '../Button';

const BV1 = props => (
  <Button
    widthType="auto"
    heightType="medium"
    roundedType="all"
    bgType="vk"
    icon="vk"
    {...props}
  />
);

export default BV1;
