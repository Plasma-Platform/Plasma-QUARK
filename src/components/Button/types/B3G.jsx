import React from 'react';

import Button from '../Button';

const B3G = props => (
  <Button
    widthType="full"
    heightType="medium"
    roundedType="all"
    bgType="3"
    {...props}
  />
);

export default B3G;
