import React from 'react';

import Button from '../Button';

const BT2 = props => (
  <Button
    widthType="full"
    heightType="medium"
    roundedType="all"
    bgType="twitter"
    icon="twitter"
    {...props}
  />
);

export default BT2;
