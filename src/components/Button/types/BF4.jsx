import React from 'react';

import Button from '../Button';

const BF4 = props => (
  <Button
    widthType="full"
    heightType="large"
    roundedType="all"
    bgType="facebook"
    icon="facebook"
    {...props}
  />
);

export default BF4;
