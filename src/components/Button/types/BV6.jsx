import React from 'react';

import Button from '../Button';

const BV6 = props => (
  <Button
    widthType="square"
    heightType="large"
    roundedType="all"
    bgType="vk"
    icon="vk"
    {...props}
  />
);

export default BV6;
