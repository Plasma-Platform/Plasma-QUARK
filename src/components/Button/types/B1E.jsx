import React from 'react';

import Button from '../Button';

const B1E = props => (
  <Button
    widthType="auto"
    heightType="medium"
    roundedType="all"
    bgType="1"
    {...props}
  />
);

export default B1E;
