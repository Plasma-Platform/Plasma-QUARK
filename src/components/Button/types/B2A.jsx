import React from 'react';

import Button from '../Button';

const B2A = props => (
  <Button
    widthType="auto"
    heightType="medium"
    roundedType="all"
    bgType="2"
    {...props}
  />
);

export default B2A;
