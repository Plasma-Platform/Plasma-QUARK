import React from 'react';

import Button from '../Button';

const B3N = props => (
  <Button
    widthType="square"
    heightType="large"
    roundedType="all"
    bgType="3"
    {...props}
  />
);

export default B3N;
