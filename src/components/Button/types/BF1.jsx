import React from 'react';

import Button from '../Button';

const BF1 = props => (
  <Button
    widthType="auto"
    heightType="medium"
    roundedType="all"
    bgType="facebook"
    icon="facebook"
    {...props}
  />
);

export default BF1;
