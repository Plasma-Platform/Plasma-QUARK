import React from 'react';

import Button from '../Button';

const B3J = props => (
  <Button
    widthType="full"
    heightType="medium"
    roundedType="bottom"
    bgType="3"
    {...props}
  />
);

export default B3J;
