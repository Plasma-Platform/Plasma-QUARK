import React from 'react';

import Button from '../Button';

const B3M = props => (
  <Button
    widthType="square"
    heightType="medium"
    roundedType="all"
    bgType="3"
    {...props}
  />
);

export default B3M;
