import React from 'react';

import Button from '../Button';

const B1C = props => (
  <Button
    widthType="full"
    heightType="medium"
    roundedType="all"
    bgType="1"
    {...props}
  />
);

export default B1C;
