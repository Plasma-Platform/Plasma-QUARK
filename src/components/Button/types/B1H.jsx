import React from 'react';

import Button from '../Button';

const B1H = props => (
  <Button
    widthType="full"
    heightType="large"
    roundedType="all"
    bgType="1"
    {...props}
  />
);

export default B1H;
