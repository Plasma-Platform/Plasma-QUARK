import React from 'react';

import Button from '../Button';

const B3A = props => (
  <Button
    widthType="auto"
    heightType="medium"
    roundedType="all"
    bgType="3"
    {...props}
  />
);

export default B3A;
