import React from 'react';

import Button from '../Button';

const BG4 = props => (
  <Button
    widthType="full"
    heightType="large"
    roundedType="all"
    bgType="google-plus"
    icon="google-plus"
    {...props}
  />
);

export default BG4;
