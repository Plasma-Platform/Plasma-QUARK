import React from 'react';

import Button from '../Button';

const BF6 = props => (
  <Button
    widthType="square"
    heightType="large"
    roundedType="all"
    bgType="facebook"
    icon="facebook"
    {...props}
  />
);

export default BF6;
