import React from 'react';

import Button from '../Button';

const B1I = props => (
  <Button
    widthType="full"
    heightType="medium"
    roundedType="bottom"
    bgType="1"
    {...props}
  />
);

export default B1I;
