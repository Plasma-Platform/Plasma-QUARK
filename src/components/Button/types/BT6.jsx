import React from 'react';

import Button from '../Button';

const BT6 = props => (
  <Button
    widthType="square"
    heightType="large"
    roundedType="all"
    bgType="twitter"
    icon="twitter"
    {...props}
  />
);

export default BT6;
