import React from 'react';

import Button from '../Button';

const B2H = props => (
  <Button
    widthType="full"
    heightType="large"
    roundedType="all"
    bgType="2"
    {...props}
  />
);

export default B2H;
