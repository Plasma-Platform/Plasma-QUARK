import React from 'react';

import Button from '../Button';

const BG2 = props => (
  <Button
    widthType="full"
    heightType="medium"
    roundedType="all"
    bgType="google-plus"
    icon="google-plus"
    {...props}
  />
);

export default BG2;
