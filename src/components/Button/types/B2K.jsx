import React from 'react';

import Button from '../Button';

const B2K = props => (
  <Button
    widthType="full"
    heightType="large"
    roundedType="bottom"
    bgType="2"
    {...props}
  />
);

export default B2K;
