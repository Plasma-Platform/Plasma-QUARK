import React from 'react';

import Button from '../Button';

const B2I = props => (
  <Button
    widthType="full"
    heightType="medium"
    roundedType="bottom"
    bgType="2"
    {...props}
  />
);

export default B2I;
