import React from 'react';

import Button from '../Button';

const BV3 = props => (
  <Button
    widthType="auto"
    heightType="large"
    roundedType="all"
    bgType="vk"
    icon="vk"
    {...props}
  />
);

export default BV3;
