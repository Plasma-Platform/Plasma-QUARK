import React from 'react';

import Button from '../Button';

const BV4 = props => (
  <Button
    widthType="full"
    heightType="large"
    roundedType="all"
    bgType="vk"
    icon="vk"
    {...props}
  />
);

export default BV4;
