import React from 'react';

import Button from '../Button';

const B1M = props => (
  <Button
    widthType="square"
    heightType="medium"
    roundedType="all"
    bgType="1"
    {...props}
  />
);

export default B1M;
