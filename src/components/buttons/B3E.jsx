import React from 'react';

import Button from '../Button';

const B3E = props => (
  <Button
    widthType="auto"
    heightType="medium"
    roundedType="all"
    bgType="3"
    {...props}
  />
);

export default B3E;
