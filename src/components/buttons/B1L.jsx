import React from 'react';

import Button from '../Button';

const B1L = props => (
  <Button
    widthType="full"
    heightType="large"
    roundedType="bottom"
    bgType="1"
    {...props}
  />
);

export default B1L;
