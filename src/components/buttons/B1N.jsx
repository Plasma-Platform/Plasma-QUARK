import React from 'react';

import Button from '../Button';

const B1N = props => (
  <Button
    widthType="square"
    heightType="large"
    roundedType="all"
    bgType="1"
    {...props}
  />
);

export default B1N;
