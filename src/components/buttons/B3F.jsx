import React from 'react';

import Button from '../Button';

const B3F = props => (
  <Button
    widthType="auto"
    heightType="large"
    roundedType="all"
    bgType="3"
    {...props}
  />
);

export default B3F;
