import React from 'react';

import Button from '../Button';

const B3L = props => (
  <Button
    widthType="full"
    heightType="large"
    roundedType="bottom"
    bgType="3"
    {...props}
  />
);

export default B3L;
