import React from 'react';

import Button from '../Button';

const BV2 = props => (
  <Button
    widthType="full"
    heightType="medium"
    roundedType="all"
    bgType="vk"
    icon="vk"
    {...props}
  />
);

export default BV2;
