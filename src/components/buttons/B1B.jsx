import React from 'react';

import Button from '../Button';

const B1B = props => (
  <Button
    widthType="auto"
    heightType="large"
    roundedType="all"
    bgType="1"
    {...props}
  />
);

export default B1B;
