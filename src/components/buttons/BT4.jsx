import React from 'react';

import Button from '../Button';

const BT4 = props => (
  <Button
    widthType="full"
    heightType="large"
    roundedType="all"
    bgType="twitter"
    icon="twitter"
    {...props}
  />
);

export default BT4;
