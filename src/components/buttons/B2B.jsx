import React from 'react';

import Button from '../Button';

const B2B = props => (
  <Button
    widthType="auto"
    heightType="large"
    roundedType="all"
    bgType="2"
    {...props}
  />
);

export default B2B;
