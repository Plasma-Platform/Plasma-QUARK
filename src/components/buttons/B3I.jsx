import React from 'react';

import Button from '../Button';

const B3I = props => (
  <Button
    widthType="full"
    heightType="medium"
    roundedType="bottom"
    bgType="3"
    {...props}
  />
);

export default B3I;
