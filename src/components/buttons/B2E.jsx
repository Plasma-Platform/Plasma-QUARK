import React from 'react';

import Button from '../Button';

const B2E = props => (
  <Button
    widthType="auto"
    heightType="medium"
    roundedType="all"
    bgType="2"
    {...props}
  />
);

export default B2E;
