import React from 'react';

import Button from '../Button';

const BF5 = props => (
  <Button
    widthType="square"
    heightType="medium"
    roundedType="all"
    bgType="facebook"
    icon="facebook"
    {...props}
  />
);

export default BF5;
